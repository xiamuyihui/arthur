server:
  port: 10000
eureka:
  instance:
    hostname: arthur-eureka
    prefer-ip-address: true
  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:
      defaultZone: http://localhost:10000/eureka/
